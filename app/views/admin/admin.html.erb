<div class="vitals-text-container">
    <h4><%= link_to "New Project", new_admin_project_path %></h4>

</div>
<br><h1>Logs</h1>
<ul>
    <% @logs.each do |log| %>
        <li><%= log.company || log.ip %> visited the <%= log.page_visited %> page on <%= log.created_at.localtime("-05:00").strftime("%m/%d/%Y at %l:%M%p") %> from <%= log.location %></li><br>
    <% end %>
</ul>
